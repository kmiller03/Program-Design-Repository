//broken, mostly unwritten

#include <stdio.h>
#define MAX_LEN 1000


int main() {
    int a[MAX_LEN];
    FILE *myFile;
    int count = 0;

    if (myFile == NULL) {
        printf("Error opening file\n");
        return 1;
    }

    while (!feof(myFile) && !ferror(myFile)) {
        if (s)
    }
    return 0;
}