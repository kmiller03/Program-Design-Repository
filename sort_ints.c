#include <stdio.h>

int int_compare(const void *p, const void *q)
{
    const int * p1 = p;
    const int * q1 = q;
    if (*p1 < *q1) {
        return -1;
    }
    else if (*pq == *q1)
}

int main()
{

    return 0;
}